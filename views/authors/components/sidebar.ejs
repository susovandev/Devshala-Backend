<aside class="w-64 bg-white shadow-lg flex flex-col justify-between">
  <div class="p-6 border-b">
    <h1 class="text-2xl font-bold text-indigo-600"><%= pageTitle %></h1>
  </div>

  <nav class="flex-1 px-4 py-6 space-y-2">
    <a
      href="/authors/dashboard"
      class="block px-4 py-2 rounded-lg transition <%= currentPath === '/authors/dashboard' ? 'bg-indigo-100 text-indigo-700 font-semibold border-l-4 border-indigo-600' : 'text-gray-700 hover:bg-indigo-50 hover:text-indigo-600' %>"
    >
      Dashboard
    </a>

    <a
      href="/authors/profile"
      class="block px-4 py-2 rounded-lg transition <%= currentPath === '/authors/profile' ? 'bg-indigo-100 text-indigo-700 font-semibold border-l-4 border-indigo-600' : 'text-gray-700 hover:bg-indigo-50 hover:text-indigo-600' %>"
    >
      Manage Profile
    </a>

    <a
      href="/authors/blogs"
      class="block px-4 py-2 rounded-lg transition <%= currentPath === '/authors/blogs' ? 'bg-indigo-100 text-indigo-700 font-semibold border-l-4 border-indigo-600' : 'text-gray-700 hover:bg-indigo-50 hover:text-indigo-600' %>"
    >
      Manage Blogs
    </a>
    <a
      href="/authors/leaderboard"
      class="block px-4 py-2 rounded-lg transition <%= currentPath === '/authors/leaderboard' ? 'bg-indigo-100 text-indigo-700 font-semibold border-l-4 border-indigo-600' : 'text-gray-700 hover:bg-indigo-50 hover:text-indigo-600' %>"
    >
      Leaderboard
    </a>
  </nav>

  <div class="p-4 border-t flex items-center justify-between gap-3">
    <div class="flex items-center gap-3">
      <div class="relative">
        <img
          src="<%= author?.avatarUrl?.url || 'https://ui-avatars.com/api/?name=' + author.username + '&background=0D8ABC&color=fff' %>"
          class="w-10 h-10 rounded-full object-cover border"
        />
        <span
          class="absolute bottom-0 right-0 w-2.5 h-2.5 bg-green-500 border-2 border-white rounded-full"
        ></span>
      </div>

      <div class="leading-tight">
        <p class="text-sm font-semibold"><%= author.username %></p>
        <p class="text-xs text-gray-500 truncate max-w-[140px]">
          <%= author.email.slice(0, 10).concat("...") %>
        </p>
        <span class="inline-block mt-1 text-xs bg-indigo-100 text-indigo-600 px-2 py-0.5 rounded">
          <%= author.role %>
        </span>
      </div>
    </div>

    <form action="/authors/auth/logout" method="POST">
      <button
        type="submit"
        class="text-red-500 hover:text-red-700 text-sm font-medium bg-red-100 px-3 py-1 rounded"
      >
        Logout
      </button>
    </form>
  </div>
</aside>
