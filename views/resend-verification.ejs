<!DOCTYPE html>
<html lang="en">
<%- include("./partials/head.ejs", { title: "Resend Verification • DevShala" }) %>

<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <%- include("./partials/toast.ejs") %>

  <!-- Subtle Background Glow -->
  <div class="fixed inset-0 -z-10 overflow-hidden">
    <svg class="absolute -bottom-40 left-0 opacity-20" width="700" height="700" fill="none">
      <defs>
        <radialGradient id="verifyGlow" cx="50%" cy="50%" r="50%">
          <stop offset="0%" stop-color="#22D3EE"/>
          <stop offset="100%" stop-color="transparent"/>
        </radialGradient>
      </defs>
      <circle cx="350" cy="350" r="350" fill="url(#verifyGlow)" />
    </svg>
  </div>

  <!-- Card -->
  <div class="w-full max-w-md bg-white rounded-2xl border shadow-sm p-8">

    <!-- Header -->
    <div class="text-center mb-8">
      <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full
                   bg-sky-50 text-sky-600 text-xs font-semibold">
        EMAIL VERIFICATION
      </span>

      <h1 class="mt-4 text-3xl font-bold text-gray-900">
        Verify your email
      </h1>

      <p class="mt-2 text-sm text-gray-600">
        Didn’t receive the verification email?
        Enter your email and we’ll resend a fresh verification link.
      </p>
    </div>

    <!-- Form -->
    <form action="/auth/resend-verification" method="POST" class="space-y-5">

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">
          Email address
        </label>
        <input
          type="email"
          name="email"
          required
          placeholder="you@company.com"
          class="w-full px-4 py-2.5 border rounded-lg text-sm
                 focus:ring-2 focus:ring-sky-500 focus:outline-none"
        />
      </div>

      <button
        type="submit"
        class="w-full bg-sky-600 text-white py-2.5 rounded-lg
               font-medium hover:bg-sky-700 transition"
      >
        Resend verification email
      </button>

    </form>

    <!-- Info Note -->
    <div class="mt-6 rounded-lg bg-blue-50 border border-blue-200 p-3
                text-xs text-blue-700 flex gap-2">
      <svg class="h-4 w-4 mt-0.5" fill="none" stroke="currentColor" stroke-width="2"
           viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round"
              d="M13 16h-1v-4h-1m1-4h.01M12 18a6 6 0 100-12 6 6 0 000 12z"/>
      </svg>
      <span>
        If the email exists and is unverified, a new verification link will be sent.
        For security, we don’t reveal account status.
      </span>
    </div>

    <!-- Footer -->
    <div class="mt-8 text-center text-sm text-gray-600">
      Already verified?
      <a href="/auth/login" class="text-sky-600 font-medium hover:underline">
        Back to login
      </a>
    </div>

  </div>

  <%- include("./partials/toast-script.ejs") %>
</body>
</html>
