<!DOCTYPE html>
<html lang="en">
<%- include('../../partials/head.ejs', {title}) %>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <%- include("../../partials/toast.ejs") %>

  <div class="w-full max-w-md bg-white rounded-2xl shadow-lg p-8">

    <!-- Logo -->
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold text-indigo-600">DevShala</h1>
      <p class="text-sm text-gray-500 mt-1">
        Welcome back, login to your account
      </p>
    </div>

    <!-- Login Form -->
    <form action="/publishers/auth/login" method="POST" class="space-y-4">

      <!-- Email -->
      <div>
        <label class="block text-sm font-medium mb-1">
          Email
        </label>
        <input
          type="email"
          name="email"
          required
          placeholder="Enter your email"
          class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none"
        />
      </div>

      <!-- Password -->
      <div>
        <label class="block text-sm font-medium mb-1">
          Password
        </label>
        <input
          type="password"
          name="password"
          required
          placeholder="Enter your password"
          class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none"
        />
      </div>

      <!-- Forgot password -->
      <div class="flex items-center justify-end">
        <a
          href="/publishers/auth/forgot-password"
          class="text-sm text-indigo-600 hover:underline"
        >
          Forgot password?
        </a>
      </div>

      <!-- Submit -->
      <button
        type="submit"
        class="w-full bg-indigo-600 text-white py-2 rounded-lg font-medium hover:bg-indigo-700 transition"
      >
        Login
      </button>

    </form>

    <!-- Divider -->
    <div class="my-6 flex items-center">
      <div class="flex-1 border-t"></div>
      <span class="px-3 text-sm text-gray-400">OR</span>
      <div class="flex-1 border-t"></div>
    </div>

    <!-- Register Home page -->
    <p class="text-center text-sm text-gray-600">
      
      <a href="/" class="text-indigo-600 font-medium hover:underline">
        
      </a>
    </p>

  </div>

<%- include("../../partials/toast-script.ejs") %>
</body>
</html>
