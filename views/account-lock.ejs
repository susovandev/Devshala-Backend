<!DOCTYPE html>
<html lang="en">
<%- include("./partials/head.ejs", { title: "Account Locked • Support" }) %>

<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <%- include("./partials/toast.ejs") %>

  <div class="w-full max-w-md bg-white rounded-2xl border shadow-sm p-8">

    <!-- Header -->
    <div class="text-center mb-6">
      <span class="inline-flex px-3 py-1 rounded-full
                   bg-red-50 text-red-600 text-xs font-semibold">
        SECURITY NOTICE
      </span>

      <h1 class="mt-3 text-2xl font-bold text-gray-900">
        Account locked
      </h1>

      <p class="mt-1 text-sm text-gray-500">
        We’ll help you restore access securely.
      </p>
    </div>

    <!-- Form -->
    <form action="/support/contact" method="POST" class="space-y-4">
      <input type="hidden" name="topic" value="account-lock" />

      <input
        type="email"
        name="email"
        required
        placeholder="Registered email"
        class="w-full px-4 py-2.5 border rounded-lg text-sm
               focus:ring-2 focus:ring-red-500 focus:outline-none"
      />

      <select
        name="lockReason"
        required
        class="w-full px-4 py-2.5 border rounded-lg text-sm bg-white
               focus:ring-2 focus:ring-red-500 focus:outline-none"
      >
        <option value="">Reason for lock</option>
        <option value="attempts">Too many login attempts</option>
        <option value="suspicious">Suspicious activity</option>
        <option value="policy">Policy issue</option>
        <option value="unknown">Not sure</option>
      </select>

      <textarea
        name="details"
        rows="3"
        placeholder="Optional details"
        class="w-full px-4 py-2.5 border rounded-lg text-sm resize-none
               focus:ring-2 focus:ring-red-500 focus:outline-none"
      ></textarea>

      <button
        type="submit"
        class="w-full bg-red-600 text-white py-2.5 rounded-lg
               font-medium hover:bg-red-700 transition"
      >
        Request unlock
      </button>
    </form>

    <!-- Footer hint -->
    <p class="mt-5 text-center text-xs text-gray-500">
      We’ll never ask for your password.
      <a href="/support" class="text-red-600 font-medium hover:underline">
        Support center
      </a>
    </p>

  </div>

  <%- include("./partials/toast-script.ejs") %>
</body>
</html>
