<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs', { title: 'About DevShala' }) %>

  <body class="bg-gray-50 text-gray-800">
    <!-- ================= HEADER ================= -->
    <header class="sticky top-0 z-50 bg-white border-b">
      <div class="max-w-7xl mx-auto px-6">
        <div class="flex items-center justify-between h-16">
          <a href="/" class="text-2xl font-bold text-indigo-600">DevShala</a>

          <nav class="hidden md:flex space-x-8">
            <a
              href="/"
              class="text-sm font-medium hover:text-indigo-600 <% if (currentPath === '/') { %> text-indigo-600 <% } %>"
            >
              Home
            </a>

            <a
              href="/about"
              class="text-sm font-medium hover:text-indigo-600 <% if (currentPath === '/about') { %> text-indigo-600 <% } %>"
            >
              About
            </a>
          </nav>

          <div class="flex items-center space-x-4">
            <% if (currentUser) { %>

            <div class="relative">
              <!-- Bell -->
              <button
                id="notification-bell"
                type="button"
                class="relative p-2 rounded-full hover:bg-gray-100 transition text-gray-600 hover:text-gray-900"
              >
                <i class="fa-regular fa-bell text-lg"></i>

                <% if (totalUnreadNotifications > 0) { %>
                <span
                  id="notification-badge"
                  class="absolute -top-1 -right-1 bg-red-500 text-white text-[10px] w-5 h-5 flex items-center justify-center rounded-full font-medium"
                >
                  <%= totalUnreadNotifications %>
                </span>
                <% } %>
              </button>

              <!-- DROPDOWN -->
              <div
                class="absolute right-0 top-full z-50 mt-1 w-48 rounded-lg border bg-white shadow-lg opacity-0 invisible translate-y-1 transition-all duration-150 ease-out group-hover:opacity-100 group-hover:visible group-hover:translate-y-0"
              >
              <% if (['admin', 'author', 'publisher', 'user'].includes(currentUser.role)) { %>
  <a href="/<%= currentUser.role %>s/profile" class="block px-4 py-2 text-sm hover:bg-gray-50">
    Profile
  </a>

  <a href="/<%= currentUser.role %>s/dashboard" class="block px-4 py-2 text-sm hover:bg-gray-50">
    Dashboard
  </a>

  <div class="border-t"></div>

  <form action="/<%= currentUser.role %>s/auth/logout" method="POST">
    <button class="w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-gray-50">
      Logout
    </button>
  </form>
<% } %>
              </div>
            </div>

            <div class="relative group">
              <button
                class="flex items-center gap-2 rounded-full px-1 py-1 hover:bg-gray-100 focus:outline-none"
              >
                <img
                  src="<%= currentUser?.avatarUrl?.url || `https://ui-avatars.com/api/?name=${currentUser?.username}&background=0D8ABC&color=fff` %>"
                  class="w-8 h-8 rounded-full object-cover"
                />
                <i class="fa-solid fa-chevron-down text-xs text-gray-500"></i>
              </button>

              <!-- DROPDOWN -->
              <div
                class="absolute right-0 top-full z-50 mt-1 w-48 rounded-lg border bg-white shadow-lg opacity-0 invisible translate-y-1 transition-all duration-150 ease-out group-hover:opacity-100 group-hover:visible group-hover:translate-y-0"
              >
                <a href="/users/profile" class="block px-4 py-2 text-sm hover:bg-gray-50">
                  Profile
                </a>

                <a href="/users/dashboard" class="block px-4 py-2 text-sm hover:bg-gray-50">
                  Dashboard
                </a>

                <div class="border-t"></div>

                <form action="/users/auth/logout" method="POST">
                  <button class="w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-gray-50">
                    Logout
                  </button>
                </form>
              </div>
            </div>

            <% } else { %>
            <a
              href="/users/auth/login"
              class="px-4 py-2 text-sm font-medium text-indigo-600 border border-indigo-600 rounded hover:bg-indigo-600 hover:text-white"
            >
              Login
            </a>
            <% } %>
          </div>
        </div>
      </div>
    </header>

    <!-- ================= HERO ================= -->
    <section class="bg-white border-b">
      <div class="max-w-7xl mx-auto px-6 py-20 text-center">
        <span
          class="inline-block mb-4 px-4 py-1 rounded-full bg-indigo-50 text-indigo-600 text-sm font-medium"
        >
          About DevShala
        </span>

        <h1 class="text-4xl md:text-5xl font-bold leading-tight mb-6">
          A platform built for
          <span class="text-indigo-600">developers</span>
          ,
          <br class="hidden md:block" />
          by developers
        </h1>

        <p class="max-w-2xl mx-auto text-lg text-gray-600">
          DevShala is a modern knowledge-sharing platform where developers write, learn, and grow
          together through high-quality technical content.
        </p>
      </div>
    </section>

    <!-- ================= MISSION ================= -->
    <section
      class="max-w-7xl mx-auto px-6 py-20 grid grid-cols-1 md:grid-cols-2 gap-14 items-center"
    >
      <div>
        <h2 class="text-3xl font-bold mb-4">Our Mission</h2>
        <p class="text-gray-600 text-lg leading-relaxed mb-6">
          The internet is full of tutorials, but not all of them are practical or trustworthy. Our
          mission is to create a clean, distraction-free platform where developers can share
          real-world experiences, system design insights, and scalable solutions.
        </p>

        <ul class="space-y-3 text-gray-600">
          <li class="flex items-start gap-3">
            <i class="fa-solid fa-check text-indigo-600 mt-1"></i>
            High-quality, peer-reviewed content
          </li>
          <li class="flex items-start gap-3">
            <i class="fa-solid fa-check text-indigo-600 mt-1"></i>
            Built for backend, frontend & system design
          </li>
          <li class="flex items-start gap-3">
            <i class="fa-solid fa-check text-indigo-600 mt-1"></i>
            No clickbait, no fluff
          </li>
        </ul>
      </div>

      <div class="relative">
        <div class="absolute -top-6 -left-6 w-full h-full bg-indigo-100 rounded-2xl"></div>
        <div class="relative bg-white rounded-2xl shadow p-8">
          <img src="/images/mission.png" alt="DevShala mission" class="w-full" />
        </div>
      </div>
    </section>

    <!-- ================= VALUES ================= -->
    <section class="bg-white border-t">
      <div class="max-w-7xl mx-auto px-6 py-20">
        <h2 class="text-3xl font-bold text-center mb-14">Our Core Values</h2>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="bg-gray-50 rounded-xl p-8 hover:shadow transition">
            <i class="fa-solid fa-code text-3xl text-indigo-600 mb-4"></i>
            <h3 class="text-xl font-semibold mb-2">Quality Engineering</h3>
            <p class="text-gray-600">
              Every article is written with production-level thinking, not toy examples.
            </p>
          </div>

          <div class="bg-gray-50 rounded-xl p-8 hover:shadow transition">
            <i class="fa-solid fa-users text-3xl text-indigo-600 mb-4"></i>
            <h3 class="text-xl font-semibold mb-2">Community First</h3>
            <p class="text-gray-600">
              We believe strong communities build better engineers and better software.
            </p>
          </div>

          <div class="bg-gray-50 rounded-xl p-8 hover:shadow transition">
            <i class="fa-solid fa-rocket text-3xl text-indigo-600 mb-4"></i>
            <h3 class="text-xl font-semibold mb-2">Continuous Growth</h3>
            <p class="text-gray-600">
              Technology evolves fast — we grow, learn, and adapt together.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- ================= STATS ================= -->
    <section class="max-w-7xl mx-auto px-6 py-20">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
        <div>
          <p class="text-4xl font-bold text-indigo-600">10K+</p>
          <p class="text-gray-500 mt-1">Monthly Readers</p>
        </div>

        <div>
          <p class="text-4xl font-bold text-indigo-600">500+</p>
          <p class="text-gray-500 mt-1">Published Blogs</p>
        </div>

        <div>
          <p class="text-4xl font-bold text-indigo-600">150+</p>
          <p class="text-gray-500 mt-1">Active Authors</p>
        </div>

        <div>
          <p class="text-4xl font-bold text-indigo-600">30+</p>
          <p class="text-gray-500 mt-1">Topics Covered</p>
        </div>
      </div>
    </section>

    <!-- ================= CTA ================= -->
    <section class="bg-indigo-600">
      <div class="max-w-7xl mx-auto px-6 py-20 text-center text-white">
        <h2 class="text-3xl font-bold mb-4">Want to write for DevShala?</h2>

        <p class="max-w-xl mx-auto mb-8 text-indigo-100">
          Share your knowledge, help others, and build your personal brand by publishing on
          DevShala.
        </p>

        <a
          href="/authors/request"
          class="inline-block px-8 py-3 bg-white text-indigo-600 font-semibold rounded-lg hover:bg-indigo-50 transition"
        >
          Become an Author
        </a>
      </div>
    </section>

    <!-- FOOTER -->
    <%- include('../partials/footer.ejs') %>
  </body>
</html>
