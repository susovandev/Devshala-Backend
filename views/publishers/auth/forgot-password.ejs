<!DOCTYPE html>
<html lang="en">
<%- include("../../partials/head.ejs", { title: "Reset Password • DevShala" }) %>

<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <%- include("../../partials/toast.ejs") %>

  <!-- Soft Background Glow -->
  <div class="fixed inset-0 -z-10 overflow-hidden">
    <svg class="absolute -top-40 right-0 opacity-20" width="700" height="700" fill="none">
      <defs>
        <radialGradient id="resetGlow" cx="50%" cy="50%" r="50%">
          <stop offset="0%" stop-color="#6366F1"/>
          <stop offset="100%" stop-color="transparent"/>
        </radialGradient>
      </defs>
      <circle cx="350" cy="350" r="350" fill="url(#resetGlow)" />
    </svg>
  </div>

  <!-- Card -->
  <div class="w-full max-w-md bg-white rounded-2xl border shadow-sm p-8">

    <!-- Header -->
    <div class="text-center mb-8">
      <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full
                   bg-indigo-50 text-indigo-600 text-xs font-semibold">
        PASSWORD RECOVERY
      </span>

      <h1 class="mt-4 text-3xl font-bold text-gray-900">
        Reset your password
      </h1>

      <p class="mt-2 text-sm text-gray-600">
        Enter the email associated with your account and we’ll send you a secure reset link.
      </p>
    </div>

    <!-- Form -->
    <form action="/publishers/auth/forgot-password" method="POST" class="space-y-5">

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">
          Email address
        </label>
        <input
          type="email"
          name="email"
          required
          placeholder="you@company.com"
          class="w-full px-4 py-2.5 border rounded-lg text-sm
                 focus:ring-2 focus:ring-indigo-500 focus:outline-none"
        />
      </div>

      <button
        type="submit"
        class="w-full bg-indigo-600 text-white py-2.5 rounded-lg
               font-medium hover:bg-indigo-700 transition"
      >
        Send reset link
      </button>

    </form>

    <!-- Security Note -->
    <div class="mt-6 rounded-lg bg-amber-50 border border-amber-200 p-3
                text-xs text-amber-700 flex gap-2">
      <svg class="h-4 w-4 mt-0.5" fill="none" stroke="currentColor" stroke-width="2"
           viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round"
              d="M12 11c0-1.657-1.343-3-3-3s-3 1.343-3 3v2h6v-2zM6 13v4a2 2 0 002 2h8a2 2 0 002-2v-4H6z"/>
      </svg>
      <span>
        If the email exists, we’ll send a reset link. For security reasons,
        we don’t confirm account availability.
      </span>
    </div>

    <!-- Footer -->
    <div class="mt-8 text-center text-sm text-gray-600">
      Remembered your password?
      <a href="/publishers/auth/login" class="text-indigo-600 font-medium hover:underline">
        Back to login
      </a>
    </div>

  </div>

  <%- include("../../partials/toast-script.ejs") %>
    <!-- BACKGROUND ANIMATION -->
  <%- include("../../partials/background.ejs") %>
</body>
</html>
