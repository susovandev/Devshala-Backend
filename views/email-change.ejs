<!DOCTYPE html>
<html lang="en">
<%- include("./partials/head.ejs", { title: "Email Change • Support" }) %>

<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <%- include("./partials/toast.ejs") %>

  <div class="w-full max-w-md bg-white rounded-2xl border shadow-sm p-8">

    <!-- Header -->
    <div class="text-center mb-6">
      <span class="inline-flex px-3 py-1 rounded-full
                   bg-indigo-50 text-indigo-600 text-xs font-semibold">
        ACCOUNT SUPPORT
      </span>

      <h1 class="mt-3 text-2xl font-bold text-gray-900">
        Change email address
      </h1>

      <p class="mt-1 text-sm text-gray-500">
        No access to your old email? We’ll help.
      </p>
    </div>

    <!-- Form -->
    <form action="/support/contact" method="POST" class="space-y-4">
      <input type="hidden" name="topic" value="email-change" />

      <input
        type="email"
        name="oldEmail"
        required
        placeholder="Current email"
        class="w-full px-4 py-2.5 border rounded-lg text-sm
               focus:ring-2 focus:ring-indigo-500 focus:outline-none"
      />

      <input
        type="email"
        name="newEmail"
        required
        placeholder="New email"
        class="w-full px-4 py-2.5 border rounded-lg text-sm
               focus:ring-2 focus:ring-indigo-500 focus:outline-none"
      />

      <textarea
        name="reason"
        rows="3"
        placeholder="Optional note (recommended)"
        class="w-full px-4 py-2.5 border rounded-lg text-sm resize-none
               focus:ring-2 focus:ring-indigo-500 focus:outline-none"
      ></textarea>

      <button
        type="submit"
        class="w-full bg-indigo-600 text-white py-2.5 rounded-lg
               font-medium hover:bg-indigo-700 transition"
      >
        Submit request
      </button>
    </form>

    <!-- Footer -->
    <p class="mt-5 text-center text-xs text-gray-500">
      Identity verification may be required.
      <a href="/support" class="text-indigo-600 font-medium hover:underline">
        Support center
      </a>
    </p>

  </div>

  <%- include("./partials/toast-script.ejs") %>
</body>
</html>
